// import { rgba } from 'polished';

const white = '#fff';
const black = '#000';
const orange = '#fea02f';

const color = {
  white,
  black,
  // black80: rgba(black, 0.8),
  orange
};

export default color;
